experiment_label: 'euroc'

settings:
  run_benchmark: true
  optional:
    dataset_first_frame: 1500
    dataset_last_frame: 1500

build_dir: '$HOME/Code/spark_vio/build'
results_dir: '$HOME/Code/spark_vio_evaluation/results'
dataset_dir: '$HOME/datasets/euroc'

datasets_to_run:
 - name: V1_01_easy
   segments: [10,20,30,40,50]
   pipelines: ['S', 'SPR']
   discard_n_start_poses: 10
   discard_n_end_poses: 10
   #- name: V1_02_medium
   #segments: [10,20,30,40,50,60]
   #pipelines: ['S', 'SPR']
   #discard_n_start_poses: 10
   #discard_n_end_poses: 10

